# 34일차

## 제약조건(Constraint) 설정 시 주의사항

### 제약조건 이름은 중복되면 안 된다

- FOREIGN KEY
- PRIMARY KEY
- UNIQUE
- CHECK
- INDEX

위 제약조건들은 **같은 데이터베이스 내에서 이름이 중복되면 오류가 발생**한다.

즉 )

- 제약조건 이름은 반드시 고유해야 한다.
- 테이블 단위가 아니라 DB 단위에서 고유하게 관리된다.
- 의미 있는 네이밍 규칙을 정해두면 관리가 쉬워진다.